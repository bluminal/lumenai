{
  "scenario": "wait-under-60s",
  "description": "Pool matches; stdout IS a TTY; wait < 60s (fast pool completes in 30s). Waiting indicator must NOT be emitted (60s threshold not crossed).",
  "setup": {
    "index_json": {
      "pools": [
        {
          "name": "review-pool-fast",
          "roster": ["code-reviewer", "security-reviewer"],
          "pool_state": "idle",
          "multi_model": false,
          "last_active_at": "2026-04-26T05:00:00Z",
          "ttl_minutes": 60,
          "metadata_dir": "~/.claude/teams/standing/review-pool-fast"
        }
      ]
    },
    "config": {
      "standing_pools": {
        "enabled": true,
        "routing_mode": "prefer-with-fallback",
        "matching_mode": "covers"
      }
    },
    "command_args": {
      "target": "staged"
    },
    "runtime": {
      "stdout_is_tty": true,
      "actual_wait_seconds": 30
    }
  },
  "expected": {
    "routing_decision": "routed-to-pool",
    "pool_name": "review-pool-fast",
    "multi_model": false,
    "waiting_indicator_emitted": false,
    "waiting_indicator_text": null,
    "submission_completed": true,
    "error": null
  }
}
