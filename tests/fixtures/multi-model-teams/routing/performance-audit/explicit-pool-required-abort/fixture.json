{
  "scenario": "explicit-pool-required-abort",
  "description": "No matching pool; routing_mode is explicit-pool-required. Command aborts with verbatim FR-MMT17 error referencing performance-engineer. No fallback occurs.",
  "setup": {
    "index_json": {
      "pools": []
    },
    "config": {
      "standing_pools": {
        "enabled": true,
        "routing_mode": "explicit-pool-required",
        "matching_mode": "covers"
      }
    },
    "command_args": {
      "scope": "full-stack"
    }
  },
  "expected": {
    "routing_decision": "skipped-routing-mode-explicit",
    "pool_name": null,
    "multi_model": null,
    "abort": true,
    "error_shown": true,
    "error_message": "No standing pool matches the required reviewers (performance-engineer).\nRouting mode is 'explicit-pool-required', so this command will not fall back to\nfresh-spawn reviewers. To proceed, either:\n  1. Start a matching pool:\n       /synthex-plus:start-review-team --reviewers performance-engineer\n  2. Change routing_mode to 'prefer-with-fallback' in .synthex-plus/config.yaml",
    "error_contains": [
      "No standing pool matches the required reviewers (performance-engineer).",
      "Routing mode is 'explicit-pool-required', so this command will not fall back to",
      "fresh-spawn reviewers. To proceed, either:",
      "/synthex-plus:start-review-team --reviewers performance-engineer",
      "Change routing_mode to 'prefer-with-fallback' in .synthex-plus/config.yaml"
    ]
  }
}
